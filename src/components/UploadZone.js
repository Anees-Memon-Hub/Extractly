export function UploadZone() {
    return `
        <div class="upload-zone">
            <div class="upload-icon">📷</div>

            <h2>Drop an image here</h2>

            <p>Paste (Ctrl + V), Drag & Drop, or Click to Upload</p>
        </div>
    `;
}